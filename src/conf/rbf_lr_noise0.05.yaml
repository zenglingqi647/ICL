inherit: 
    - base.yaml

training:
    task: noisy_rbf_logistic_regression
    task_kwargs:
        train_noise_prob: 0.05
    curriculum:
        points:
            start: 11
            end: 41
            inc: 2
            interval: 2000

out_dir: ../models/rbf_lr_noise0.05

wandb:
    name: "rbf_lr_noise0.05"
